import {Stylesheet} from 'react-native';

const AppStyles = Stylesheet.create({
    TInput: {
        borderRadius: 9,
        width: 300,
        height: 44,
        padding: 10,
        borderWidth: 1,
        borderColor: 'black',
        marginBottom: 10,
      },
});

export default AppStyles;